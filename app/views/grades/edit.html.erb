<%= render 'shared/de_nav_links',:active => "grades" %>
<div class="span6">
  <%= simple_form_for @grade, :html => { :class => 'form-horizontal' } do |f| %>
  <legend> Edit Grade </legend>
    <%= render 'grade_fields', :f => f %>
    <div class="form-actions">
  	  <%= render 'shared/buttons', :obj => @grade, :buttons => {'submit' => "Update", "cancel" => "Cancel" }%>
    </div>  
  <% end %>
  <%= render 'shared/controller_action_buttons', :obj => @grade, :action => "edit" %>  
</div>
<div class="span3">
  <%= render 'shared/semi_index', :model_name => "Grade",
																:columns => ['name', 'cut_off_percentage'] %>
</div>