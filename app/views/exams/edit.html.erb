<%= render 'shared/de_nav_links',:active => "exams" %>
<div class="span6">
  <%= simple_form_for @exam, :html => { :class => 'form-horizontal' } do |f| %>
  <legend> Edit Exam </legend>
    <%= render 'exam_fields', :f => f %>
    <div class="form-actions">
  	  <%= render 'shared/buttons', :obj => @exam, :buttons => {'submit' => "Update", "cancel" => "Cancel" }%>
    </div>  
  <% end %>
  <%= render 'shared/controller_action_buttons', :obj => @exam, :action => "edit" %>  
</div>
<div class="span3">
  <%= render 'shared/semi_index', :model_name => "Exam",
																:columns => ['name', 'code'] %>
</div>