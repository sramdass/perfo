<%= render 'shared/de_nav_links',:active => "exams" %>
<div class="span6">
  <%= simple_form_for @exam, :html => { :class => 'form-horizontal' } do |f| %>
  <legend> New Exam </legend>
    <%= render 'exam_fields', :f => f %>
    <div class="form-actions">
  	  <%= render 'shared/buttons', :obj => @exam, :buttons => {'submit' => "Create",  "create_and_add" => "Create and Repeat", "cancel" => "Cancel" }%>
    </div>  
  <% end %>
  <%= render 'shared/controller_action_buttons', :obj => @exam, :action => "new" %>  
</div>
<div class="span3">
  <%= render 'shared/semi_index', :model_name => "Exam",
																:columns => ['name', 'code'] %>
</div>