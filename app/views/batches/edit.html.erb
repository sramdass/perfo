<%= render 'shared/de_nav_links', :active => "batches" %>
<div class="span6">
  <%= simple_form_for @batch, :html => { :class => 'form-horizontal' } do |f| %>
  <legend> Edit Batch </legend>
    <%= render 'batch_fields', :f => f %>
    <div class="form-actions">
  	  <%= render 'shared/buttons', :obj => @batch, :buttons => {'submit' => "Update", "cancel" => "Cancel" }%>
    </div>  
  <% end %>
  <%= render 'shared/controller_action_buttons', :obj => @batch, :action => "edit" %>    
</div>
<div class="span3">
  <%= render 'shared/semi_index', :model_name => "Batch",
																:columns => ['name', 'code'] %>
</div>