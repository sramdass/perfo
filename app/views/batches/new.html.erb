<%= render 'shared/de_nav_links', :active => "batches" %>
<div class="span6">
  <%= simple_form_for @batch, :html => { :class => 'form-horizontal' } do |f| %>
  <legend> New Batch </legend>
    <%= render 'batch_fields', :f => f %>
    <div class="form-actions">
  	  <%= render 'shared/buttons', :obj => @batch, :buttons => {'submit' => "Create",  "create_and_add" => "Create and Repeat", "cancel" => "Cancel" }%>
    </div>  
  <% end %>
  <%= render 'shared/controller_action_buttons', :obj => @batch, :action => "new" %>  
</div>
<div class="span3">
  <%= render 'shared/semi_index', :model_name => "Batch",
																:columns => ['name', 'code'] %>
</div>