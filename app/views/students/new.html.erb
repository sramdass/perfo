<%= render 'shared/de_nav_links', :active => "students" %>
<div class="span6">
  <%= simple_form_for @student, :html => { :class => 'form-horizontal' } do |f| %>
  <legend> New Student </legend>
    <%= render 'student_fields', :f => f %>
    <div class="form-actions">
  	  <%= render 'shared/buttons', :obj => @student, :buttons => {'submit' => "Create",  "create_and_add" => "Create and Repeat", "cancel" => "Cancel" }%>
    </div>  
  <% end %>
<%= render 'shared/controller_action_buttons', :obj => @student, :action => "new" %>    
</div>
<div class="span3">
  <%= render 'shared/semi_index', :model_name => "Student",
																:columns => ['name', 'id_no'] %>
</div>