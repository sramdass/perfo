<%= render 'shared/de_nav_links', :active => "associate_batches" %>
<div class="span6">
  <%= render 'shared/de_batches_nav_pills', :active => "students"%>
  <%= simple_form_for @student, :html => { :class => 'form-horizontal' } do |f| %>
  <legend> Edit Student </legend>
    <%= render 'student_fields', :f => f %>
    <div class="form-actions">
  	  <%= render 'shared/buttons', :obj => @student, :buttons => {'submit' => "Update", "cancel" => "Cancel" }%>
    </div>  
  <% end %>
<%= render 'shared/controller_action_buttons', :obj => @student, :action => "edit" %>    
</div>
<div class="span3">
  <%= render 'shared/semi_index', :model_name => "Student",
																:columns => ['name', 'id_no'] %>
</div>