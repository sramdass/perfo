<%= render 'shared/admin_nav_links',:active => "roles" %>
<div class="span9">
  <%= simple_form_for @role, :html => { :class => 'form-horizontal' } do |f| %>
  <legend> Edit Role </legend>
    <%= render 'role_fields', :f => f %>
    <div class="form-actions">
  	  <%= render 'shared/buttons', :obj => @role, :buttons => {'submit' => "Update", "cancel" => "Cancel" }%>
    </div>  
  <% end %>
  <%= render 'shared/controller_action_buttons', :obj => @role, :action => "edit" %>  
</div>


