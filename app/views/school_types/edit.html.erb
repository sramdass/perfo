<%= render 'shared/de_nav_links', :active => "school_types" %>
<div class="span6">
  <%= simple_form_for @school_type, :html => { :class => 'form-horizontal' } do |f| %>
  <legend> New School Type </legend>
    <%= render 'school_type_fields', :f => f %>
    <div class="form-actions">
  	  <%= render 'shared/buttons', :obj => @school_type, :buttons => {'submit' => "Update", "cancel" => "Cancel" }%>
    </div>  
  <% end %>
  <%= render 'shared/controller_action_buttons', :obj => @school_type, :action => "new" %>  
</div>
<div class="span3">
  <%= render 'shared/semi_index', :model_name => "SchoolType",
																:columns => ['name'] %>
</div>