<%= render 'shared/de_nav_links', :active => "blood_groups" %>
<div class="span6">
  <%= simple_form_for @blood_group, :html => { :class => 'form-horizontal' } do |f| %>
  <legend> New Blood Group </legend>
    <%= render 'blood_group_fields', :f => f %>
    <div class="form-actions">
  	  <%= render 'shared/buttons', :obj => @blood_group, :buttons => {'submit' => "Create",  "create_and_add" => "Create and Repeat", "cancel" => "Cancel" }%>
    </div>  
  <% end %>
  <%= render 'shared/controller_action_buttons', :obj => @blood_group, :action => "new" %>  
</div>
<div class="span3">
  <%= render 'shared/semi_index', :model_name => "BloodGroup",
																:columns => ['name'] %>
</div>