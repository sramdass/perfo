<%= render 'shared/nav_links', :for_role => 'data_entry', :active => "subjects" %>
<div class="span6">
  <%= simple_form_for @subject, :html => { :class => 'form-horizontal' } do |f| %>
  <legend> New Subject </legend>
    <%= render 'subject_fields', :f => f %>
    <div class="form-actions">
  	  <%= render 'shared/buttons', :obj => @subject, :buttons => {'submit' => "Create",  "create_and_add" => "Create and Repeat", "cancel" => "Cancel" }%>
    </div>  
  <% end %>
</div>
<div class="span3">
  <%= render 'shared/semi_index', :model_name => "Subject",
																:columns => ['name', 'code'] %>
</div>